[project]
name = "fleek-nix-darwin"
revision = "1.0"

[profiles.default]
HOMEBREW_GITHUB_API_TOKEN = { description = "GitHub token used by Homebrew to fetch release assets from LEGO's private tap (lego/tap: bob-cli, mdc). Needs `repo` scope on a token that can read the LEGO org.", required = true }
